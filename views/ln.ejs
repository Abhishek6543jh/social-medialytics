<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Channel Data</title>
    <style>
        body.light-mode {
            background-color: #fff;
            color: #000;
        }

        /* Dark Mode Styles */
        body.dark-mode {
            background-color: #000;
            color: #fff;
        }
        .navbar {
            background-color: #007bff;
            color: #fff;
            padding: 10px 0;
            text-align: center;
        }

        .navbar ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
        }

        .navbar li {
            display: inline;
            margin: 0 20px;
        }

        .navbar a {
            text-decoration: none;
            color: #fff;
            font-weight: bold;
            font-size: 18px;
        }

        .navbar a:hover {
            text-decoration: underline;
        }

        /* Form Container Styles */
        .usernameandchannelidformcontainer {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }

        .usernameandchannelidformcontainer form {
            background-color: #f3f3f3;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 20px 20px rgba(255, 255, 255, 0);
            text-align: center;
        }

        .usernameandchannelidformcontainer label,
        .usernameandchannelidformcontainer input,
        .usernameandchannelidformcontainer button {
            display: block;
            margin-bottom: 10px;
        }

        .usernameandchannelidformcontainer label {
            font-weight: bold;
        }

        .usernameandchannelidformcontainer input[type="text"] {
            width: 100%;
            padding: 10px;
            border: 1px solid #18c3b8;
            border-radius: 5px;
            font-size: 16px;
        }

        .usernameandchannelidformcontainer button[type="submit"] {
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            padding: 10px 20px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .usernameandchannelidformcontainer button[type="submit"]:hover {
            background-color: #0056b3;
        }

        /* User Card Styles */
        .user-card {
            max-width: 500px;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 20px;
            padding: 20px;
            background-color: #f3f3f3;
            border-radius: 10px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            text-align: center;
            color: #333;
        }

        .user-avatar img {
            width: 150px;
            height: 150px;
            border-radius: 50%;
        }

        .username {
            font-size: 24px;
            font-weight: bold;
        }

        /* Card Styling */
        .card {
            flex: 1;
            border: 1px solid #ccc;
            padding: 20px;
            box-shadow: 0 0 10px rgba(15, 207, 224, 0.539);
            max-width: 200px;
            gap: 20px;
            background-color: #fff;
            border-radius: 10px;
            transition: transform 0.3s, box-shadow 0.3s;
            text-align: center;
        }

        .card:hover {
            transform: scale(1.05);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        .card-heading {
            font-size: 18px;
            margin: 0;
        }

        .card-content {
            font-size: 24px;
            margin-top: 10px;
        }

        /* Instagram Container Styles */
        .insta-container {
            border: 1px solid #ccc;
            padding: 10px;
        }

        /* Instagram Section Styling */
        .insta-container h1 {
            text-align: center;
            font-size: 28px;
            font-weight: bold;
            color: #333;
        }

        /* YouTube Container Styles */
        .youtube-container {
            border: 1px solid #ccc;
            padding: 10px;
            margin-top: 20px;
        }

        /* YouTube Section Styling */
        .youtube-container h1 {
            text-align: center;
            font-size: 28px;
            font-weight: bold;
            color: #333;
        }

        /* Toggle Switch Styles */
        .toggle-switch {
            text-align: center;
            margin-top: 20px;
        }

        /* Dark Mode Toggle Styles */
        #dark-mode-toggle {
            display: none;
        }

        .toggle-switch label {
            cursor: pointer;
            background-color: #007bff;
            color: #fff;
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: bold;
        }

        #dark-mode-toggle:checked + label {
            background-color: #333;
        }
      

        /* Container for Instagram and YouTube sections */
        .sections-container {
            display: flex;
            flex-wrap: wrap; /* Allow cards to wrap to the next line if necessary */
            gap: 20px; /* Adjust the gap between cards as needed */
            justify-content: center; /* Center cards horizontally */
            margin-top: 20px;
        }

        /* Instagram and YouTube sections */
        .insta-container, .youtube-container {
            flex: 1; /* Make both sections equally wide */
            min-width: 300px; /* Adjust the minimum width of each section as needed */
            max-width: 500px;
        }
    </style>
</head>
<body class="light-mode">
    <div class="main-container">
        <nav class="navbar">
            <ul>
                <li><a href="#">Home</a></li>
                <li><a href="#">About</a></li>
                <li><a href="#">Services</a></li>
                <li><a href="#">Contact</a></li>
            </ul>
        </nav>
        <div class="toggle-switch">
            <input type="checkbox" id="dark-mode-toggle">
            <label for="dark-mode-toggle">Dark Mode</label>
        </div>
        <div class="usernameandchanneidformcointainer">
            <form method="POST" action="http://localhost:3000/dataobtain">
                <label for="instagramUsername">Instagram Channel Name:</label>
                <input type="text" id="instagramUsername" name="instagramUsername" required placeholder="Instagram @username">

                <label for="youtubeChannelId">YouTube Channel ID:</label>
                <input type="text" id="youtubeChannelId" name="youtubeChannelId" required placeholder="YouTube channel-id">
                <button type="submit">Analyze</button>
                <p id="wait-message" style="display: none;">Please wait for a few minutes...</p>
            </form>
        </div>
        <!-- Container for Instagram and YouTube sections -->
        <div class="sections-container">
            <!-- Instagram Section -->
            <div class="insta-container">
                <section class="content">
                        <h1>Instagram Data</h1>
                        <div class="user-card">
                            <div class="user-avatar">
                                <img src="https://www.pngmart.com/files/23/Insta-Logo-PNG-File.png" alt="User Avatar">
                            </div>
                            <div class="username">
                                <% if (locals.instagramData) { %>
                                    @<%= locals.instagramData.username %>
                                <% } %>
                            </div>
                            <div class="row">
                                <div class="card">
                                    <h2 class="card-heading">Followers</h2>
                                    <p class="card-content">
                                        <% if (locals.instagramData) { %>
                                            <%= locals.instagramData.followers %>
                                        <% } %>
                                    </p>
                                </div>
                                <div class="card">
                                    <h2 class="card-heading">Total Likes</h2>
                                    <p class="card-content">
                                        <% if (locals.instagramData) { %>
                                            <%= locals.instagramData.total_likes %>
                                        <% } %>
                                    </p>
                                </div>
                            </div>
                            <div class="row">
                                <div class="card">
                                    <h2 class="card-heading">Total Comments</h2>
                                    <p class="card-content">
                                        <% if (locals.instagramData) { %>
                                            <%= locals.instagramData.total_comments %>
                                        <% } %>
                                    </p>
                                </div>
                                <div class="card">
                                    <h2 class="card-heading">No. of Posts</h2>
                                    <p class="card-content">
                                        <% if (locals.instagramData) { %>
                                            <%= locals.instagramData.total_posts %>
                                        <% } %>
                                    </p>
                                </div>
                            </div>
                            <h1>Engagement Rate</h1>
                            <p>
                                <% if (locals.instagramData) { %>
                                    <%= locals.instagramData.engagement_rate %>
                                <% } %>
                            </p>
                        </div>
                    
                </section>
            </div>
            <!-- YouTube Section -->
            <div class="youtube-container">
                <section class="content">
                 
                        <h1>YouTube Data</h1>
                        <div class="user-card">
                            <div class="user-avatar">
                                <img src="https://cdn.pixabay.com/photo/2021/12/10/16/38/youtube-6860916_1280.png" alt="User Avatar">
                            </div>
                            <div class="username">
                                <% if (locals.youtubeData) { %>
                                    <%= locals.youtubeData.channelName %>
                                <% } %>
                            </div>
                            <div class="row">
                                <div class="card">
                                    <h2 class="card-heading">Subscribers</h2>
                                    <p class="card-content">
                                        <% if (locals.youtubeData) { %>
                                            <%= locals.youtubeData.subscribersCount %>
                                        <% } %>
                                    </p>
                                </div>
                                <div class="card">
                                    <h2 class="card-heading">Avg Likes</h2>
                                    <p class="card-content">
                                        <% if (locals.youtubeData) { %>
                                            <%= locals.youtubeData.averagePostLikes %>
                                        <% } %>
                                    </p>
                                </div>
                            </div>
                            <div class="row">
                                <div class="card">
                                    <h2 class="card-heading">Avg Comments</h2>
                                    <p class="card-content">
                                        <% if (locals.youtubeData) { %>
                                            <%= locals.youtubeData.averagePostComments %>
                                        <% } %>
                                    </p>
                                </div>
                                <div class="card">
                                    <h2 class="card-heading">No. of Posts</h2>
                                    <p class="card-content">
                                        <% if (locals.youtubeData) { %>
                                            <%= locals.youtubeData.numberOfPosts %>
                                        <% } %>
                                    </p>
                                </div>
                            </div>
                            <h1>Engagement Rate</h1>
                            <p>
                                <% if (locals.youtubeData) { %>
                                    <%= locals.youtubeData.engagementRate %>
                                <% } %>
                            </p>
                        </div>
                    
                </section>
            </div>
        </div>
    </div>
    <script>
        // Function to toggle between light and dark mode
        function toggleDarkMode() {
            const body = document.body;
            const darkModeToggle = document.getElementById('dark-mode-toggle');

            darkModeToggle.addEventListener('change', () => {
                if (darkModeToggle.checked) {
                    body.classList.add('dark-mode');
                    body.classList.remove('light-mode');
                    localStorage.setItem('theme', 'dark');
                } else {
                    body.classList.add('light-mode');
                    body.classList.remove('dark-mode');
                    localStorage.setItem('theme', 'light');
                }
            });

            // Check the user's preference from the previous session (if any)
            const userPreference = localStorage.getItem('theme');
            if (userPreference === 'dark') {
                darkModeToggle.checked = true;
                body.classList.add('dark-mode');
            } else {
                body.classList.add('light-mode');
            }
        }

        // Call the function to initialize the toggle
        toggleDarkMode();

        // Function to handle form submission
        function handleSubmit() {
            // Show the "Please wait" message
            const waitMessage = document.getElementById('wait-message');
            waitMessage.style.display = 'block';

            // Disable the "Analyze" button to prevent multiple submissions
            const analyzeButton = document.getElementById('analyze-button');
            analyzeButton.disabled = true;

            // You can continue with the form submission here...
        }

        // Attach the handleSubmit function to the form submission
        const form = document.querySelector('form');
        form.addEventListener('submit', handleSubmit);
    </script>
</body>
</html>
